name: trapendenv10
description: TrapendenV10
publish_to: "none"

version: 10.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.5.3

  cupertino_icons: ^1.0.8
  video_player: ^2.10.0
  marquee: ^2.3.0
  font_awesome_flutter: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/videos/

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/nezee.png"
